module.exports.config = {
	name: "tezt",
	version: "1.0.0",
	hasPermssion: 0,
	credits: "DungUwU",
	description: "just tezt",
	commandCategory: "general",
	usages: "tezt",
	cooldowns: 1,
	dependencies: ['request', 'fs-extra']
};

const fs = require("fs");
module.exports.run = ({ api, event, args, utils }) => {
	if (!args[0]) api.sendMessagee('cc thieu kia', event.threadID, event,messageID); else {
		switch(args[0]) {
			case
		}
	}
}
